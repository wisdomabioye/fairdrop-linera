import CreatorGetStarted from "@/app-dashboard/get-started";

export const dynamic = 'force-static';
export const revalidate = false;

export default function CreatorGetStartedPage() {
  return <CreatorGetStarted />;
}